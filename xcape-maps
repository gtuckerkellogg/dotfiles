# Install xcape from apt or from https://github.com/alols/xcape
# use left control as escape when pressed and released
# emacs and vim users will like this

xcape

# Use return as control when pressed and held.  Emacs users will love this

# xmodmap -e "remove Control = Control_R"
# xmodmap -e "keycode 0x69 = Return"
# xmodmap -e "keycode 0x24 = Control_R"
# xmodmap -e "add Control = Control_R"

# xcape -t 10000 -e "Control_R=Return"

